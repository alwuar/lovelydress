import './bootstrap'; // El archivo de Laravel para Axios

import $ from 'jquery';
import Popper from 'popper.js';

window.$ = window.jQuery = $;
window.Popper = Popper;

// Importamos el JS de Bootstrap 4
import 'bootstrap';